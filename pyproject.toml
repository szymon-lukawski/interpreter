[project]
name = "tkom-projekt-sl"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "multipledispatch>=1.0.0",
]

[dependency-groups]
dev = [
    "pytest>=8.3.4",
]
